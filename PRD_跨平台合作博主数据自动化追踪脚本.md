# 跨平台合作博主数据自动化追踪脚本（PRD）

## 1. 项目背景与目标

### 1.1 背景
目前合作博主内容发布后，运营需要手动在小红书、抖音、B站等平台逐条查看并记录播放、点赞、评论等数据。该流程存在以下问题：
- 人工重复劳动强，效率低；
- 多平台切换易漏记、错记；
- 历史数据不连续，ROI追踪滞后。

### 1.2 目标
开发一套可维护的自动化采集脚本，基于已提供的帖子链接批量抓取核心互动数据，并输出标准化报表（Excel/CSV），用于合作效果追踪与ROI分析。

### 1.3 成功指标（建议）
- 单次支持至少 100 条链接批量处理；
- 有效链接抓取成功率 ≥ 90%（不含验证码阻断与权限受限场景）；
- 报表生成时长 ≤ 20 分钟（100 条链接，常规网络环境）；
- 同一链接历史数据可按天/次追加，便于趋势对比。

---

## 2. 适用范围与边界

### 2.1 支持平台（Phase 1）
- 小红书
- 哔哩哔哩（B站）
- 抖音

### 2.2 不在范围（Phase 1）
- 手机端 App 内私有接口逆向；
- 绕过平台安全机制（如强制破解验证码）；
- 需要平台创作者后台权限的私有指标（如部分阅读明细）。

---

## 3. 核心功能需求

## 3.1 输入模块（配置源）

### 3.1.1 输入方式
支持以下两种输入格式：
1. Excel（`.xlsx`）
2. 文本（`.txt`，每行一条记录）

### 3.1.2 字段定义
| 字段名 | 必填 | 说明 |
|---|---:|---|
| 博主昵称 | 否 | 用于报表展示，可为空 |
| 平台 | 否 | 可自动识别；若填写则用于校验 |
| 帖子链接/视频ID | 是 | 主键字段，必须有效 |
| 发布时间 | 否 | 允许为空；用于后续筛选分析 |
| 备注 | 否 | 自定义扩展字段 |

### 3.1.3 批量能力
- 支持一次导入多条链接并串行/限速并发抓取；
- 对重复链接自动去重（可配置：保留首次或最后一次）。

---

## 3.2 数据采集模块（抓取规则）

### 3.2.1 平台识别
- 根据 URL 域名和路径规则自动识别平台；
- 无法识别时标记失败原因：`平台识别失败`。

### 3.2.2 抓取字段
| 平台 | 必选指标 | 可选/进阶指标 |
|---|---|---|
| 小红书 | 点赞数、评论数、收藏数 | 分享数、阅读量（需权限） |
| B站 | 播放量、点赞数、评论数 | 投币数、收藏数、转发数、弹幕数 |
| 抖音 | 播放量、点赞数、评论数 | 收藏数、转发数 |

### 3.2.3 指标标准化映射
统一输出字段：
- 播放/阅读量（`view_count`）
- 点赞数（`like_count`）
- 评论数（`comment_count`）
- 收藏数（`favorite_count`）
- 转发数（`share_count`）

不支持或不可见字段统一置空，并在状态中说明。

---

## 3.3 数据处理与输出模块

### 3.3.1 输出格式
- `.xlsx`（默认）
- `.csv`

### 3.3.2 表头规范
`更新时间 | 博主昵称 | 平台 | 帖子链接 | 播放/阅读量 | 点赞数 | 评论数 | 收藏数 | 转发数 | 抓取状态 | 失败原因`

### 3.3.3 更新机制
- 支持 `append` 追加写入历史记录；
- 每次抓取写入独立“更新时间”（精确到秒）；
- 支持基于“链接 + 更新时间”去重策略（可配置）。

---

## 4. 技术与非功能性需求（重点）

## 4.1 技术栈建议
- 语言：Python 3.11+
- 自动化框架：Playwright（优先）或 Selenium
- 数据处理：pandas/openpyxl
- 配置管理：YAML/ENV

> 不建议将 `requests + 逆向` 作为主路径，维护成本与失效率通常更高。

## 4.2 身份验证（Cookie 管理）
- 支持外部导入 Cookie（按平台分别管理）；
- 支持 Cookie 失效检测与到期提示；
- 支持“人工登录后持久化会话”以降低反复登录成本。

## 4.3 反爬虫与风控应对
- 请求节流：每条任务随机休眠 5–15 秒；
- 并发控制：默认低并发（建议 1–3）；
- User-Agent 与浏览行为模拟（可配置）；
- 遇到验证码：
  - 标记为 `抓取失败：触发验证码`；
  - 跳过继续执行，不中断整批任务。

## 4.4 稳定性与容错
- 单条失败不影响整体任务；
- 页面结构变更、链接失效、网络超时均需捕获；
- 失败原因需可审计（例如：`元素定位失败`、`404`、`Cookie失效`）。

## 4.5 合规与安全
- 遵守目标平台服务条款与法律法规；
- 不采集隐私数据，不进行越权访问；
- Cookie 等敏感信息需加密存储或安全托管；
- 日志脱敏，禁止明文输出凭据。

---

## 5. 用户交互需求

## 5.1 CLI（Phase 1 必做）
示例流程：
1. `读取输入文件` → 2. `逐条抓取` → 3. `生成报表`

示例提示语：
- `正在读取链接...`
- `抓取中 (12/100)...`
- `抓取完成，已生成 report_2026-03-26.xlsx`

## 5.2 本地 GUI（Phase 2 可选）
- 框架：Tkinter 或 PyQt
- 最小能力：
  - 文件选择器
  - “开始抓取”按钮
  - 进度条
  - 日志窗口

---

## 6. 配置项设计（建议）

```yaml
input:
  file_path: ./input_links.xlsx
  sheet_name: Sheet1
output:
  dir: ./reports
  format: xlsx
  mode: append
crawler:
  headless: false
  min_sleep_sec: 5
  max_sleep_sec: 15
  max_workers: 2
auth:
  xhs_cookie_file: ./cookies/xhs.json
  douyin_cookie_file: ./cookies/douyin.json
  bilibili_cookie_file: ./cookies/bilibili.json
logging:
  level: INFO
  file: ./logs/run.log
```

---

## 7. 验收标准（UAT）
- 能正确读取输入文件并识别平台；
- 三平台至少各 5 条样例链接抓取成功（在账号与网络可用前提下）；
- 输出报表字段齐全，失败项有明确原因；
- 任务中断后可重跑，且不会破坏既有历史数据；
- 对 Cookie 失效、验证码、无效链接等场景有可预期行为。

---

## 8. 里程碑与交付物（建议）

### M1（1 周）
- 完成输入/输出框架、平台识别、基础日志

### M2（1–2 周）
- 完成三平台核心字段抓取、容错与限速

### M3（1 周）
- 完成稳定性优化、UAT、部署与使用文档

### 交付物
- 源代码仓库
- 可执行脚本与配置模板
- 使用说明书（含 Cookie 导入步骤）
- 样例输入与样例报表
- 常见故障排查文档

---

## 9. Phase 2 迭代建议
- 定时任务（每日固定时间自动运行）
- 飞书/钉钉/企业微信机器人自动推送日报
- 数据看板（如按博主/平台/时间维度趋势图）
- 抓取策略热更新（应对页面结构频繁变化）

---

## 10. 给开发团队的关键提醒
- 该类抓取项目是“持续维护型项目”，不是一次性交付；
- 平台页面结构与风控策略变化频繁，建议预留月度维护预算；
- 优先保证“可观测性 + 可恢复性 + 可维护性”，其次才是抓取速度。
